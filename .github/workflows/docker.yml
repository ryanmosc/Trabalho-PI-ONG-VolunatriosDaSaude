name: Build e Push Docker

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Login no Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build da imagem
        run: docker build -t ryanmosc/front-voluntarios:${{ github.run_number }} .

      - name: Tag latest
        run: docker tag ryanmosc/front-voluntarios:${{ github.run_number }} ryanmosc/front-voluntarios:latest

      - name: Push da imagem
        run: |
          docker push ryanmosc/front-voluntarios:${{ github.run_number }}
          docker push ryanmosc/front-voluntarios:latest